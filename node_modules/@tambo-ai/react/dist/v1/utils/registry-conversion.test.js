"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const zod_1 = require("zod");
const registry_conversion_1 = require("./registry-conversion");
describe("registry-conversion", () => {
    describe("toAvailableComponent", () => {
        it("converts a component with propsSchema", () => {
            const component = {
                name: "WeatherCard",
                description: "Displays weather information",
                component: () => null,
                propsSchema: zod_1.z.object({
                    city: zod_1.z.string(),
                    temperature: zod_1.z.number(),
                }),
            };
            const result = (0, registry_conversion_1.toAvailableComponent)(component);
            expect(result.name).toBe("WeatherCard");
            expect(result.description).toBe("Displays weather information");
            expect(result.propsSchema).toBeDefined();
            expect(result.propsSchema.type).toBe("object");
        });
        it("throws when propsSchema is missing", () => {
            const component = {
                name: "BadComponent",
                description: "Missing schema",
                component: () => null,
            };
            expect(() => (0, registry_conversion_1.toAvailableComponent)(component)).toThrow('Component "BadComponent" missing propsSchema');
        });
    });
    describe("toAvailableComponents", () => {
        it("converts a Record of components", () => {
            const components = {
                Card: {
                    name: "Card",
                    description: "A card component",
                    component: () => null,
                    propsSchema: zod_1.z.object({ title: zod_1.z.string() }),
                },
                Button: {
                    name: "Button",
                    description: "A button component",
                    component: () => null,
                    propsSchema: zod_1.z.object({ label: zod_1.z.string() }),
                },
            };
            const result = (0, registry_conversion_1.toAvailableComponents)(components);
            expect(result).toHaveLength(2);
            expect(result.map((c) => c.name)).toContain("Card");
            expect(result.map((c) => c.name)).toContain("Button");
        });
        it("converts a Map of components", () => {
            const components = new Map([
                [
                    "Card",
                    {
                        name: "Card",
                        description: "A card",
                        component: () => null,
                        propsSchema: zod_1.z.object({ title: zod_1.z.string() }),
                    },
                ],
            ]);
            const result = (0, registry_conversion_1.toAvailableComponents)(components);
            expect(result).toHaveLength(1);
            expect(result[0].name).toBe("Card");
        });
        it("skips invalid components and logs warning", () => {
            const warnSpy = jest.spyOn(console, "warn").mockImplementation(() => { });
            const components = {
                Valid: {
                    name: "Valid",
                    description: "Valid component",
                    component: () => null,
                    propsSchema: zod_1.z.object({ x: zod_1.z.string() }),
                },
                Invalid: {
                    name: "Invalid",
                    description: "Missing schema",
                    component: () => null,
                },
            };
            const result = (0, registry_conversion_1.toAvailableComponents)(components);
            expect(result).toHaveLength(1);
            expect(result[0].name).toBe("Valid");
            expect(warnSpy).toHaveBeenCalledWith(expect.stringContaining('Skipping component "Invalid"'));
            warnSpy.mockRestore();
        });
    });
    describe("toAvailableTool", () => {
        it("converts a tool with inputSchema", () => {
            const tool = {
                name: "get_weather",
                description: "Gets weather for a city",
                tool: async () => ({ temp: 72 }),
                inputSchema: zod_1.z.object({ city: zod_1.z.string() }),
                outputSchema: zod_1.z.any(),
            };
            const result = (0, registry_conversion_1.toAvailableTool)(tool);
            expect(result.name).toBe("get_weather");
            expect(result.description).toBe("Gets weather for a city");
            expect(result.inputSchema).toBeDefined();
            expect(result.inputSchema.type).toBe("object");
        });
        it("throws when schema is missing", () => {
            const tool = {
                name: "bad_tool",
                description: "Missing schema",
                tool: async () => ({}),
            };
            expect(() => (0, registry_conversion_1.toAvailableTool)(tool)).toThrow('Tool "bad_tool" missing inputSchema or toolSchema');
        });
    });
    describe("toAvailableTools", () => {
        it("converts a Record of tools", () => {
            const tools = {
                search: {
                    name: "search",
                    description: "Search the web",
                    tool: async () => [],
                    inputSchema: zod_1.z.object({ query: zod_1.z.string() }),
                    outputSchema: zod_1.z.any(),
                },
                calculate: {
                    name: "calculate",
                    description: "Calculate expression",
                    tool: async () => 0,
                    inputSchema: zod_1.z.object({ expr: zod_1.z.string() }),
                    outputSchema: zod_1.z.any(),
                },
            };
            const result = (0, registry_conversion_1.toAvailableTools)(tools);
            expect(result).toHaveLength(2);
            expect(result.map((t) => t.name)).toContain("search");
            expect(result.map((t) => t.name)).toContain("calculate");
        });
        it("converts a Map of tools", () => {
            const tools = new Map([
                [
                    "fetch",
                    {
                        name: "fetch",
                        description: "Fetch data",
                        tool: async () => null,
                        inputSchema: zod_1.z.object({ url: zod_1.z.string() }),
                        outputSchema: zod_1.z.any(),
                    },
                ],
            ]);
            const result = (0, registry_conversion_1.toAvailableTools)(tools);
            expect(result).toHaveLength(1);
            expect(result[0].name).toBe("fetch");
        });
        it("skips invalid tools and logs warning", () => {
            const warnSpy = jest.spyOn(console, "warn").mockImplementation(() => { });
            const tools = {
                valid: {
                    name: "valid",
                    description: "Valid tool",
                    tool: async () => null,
                    inputSchema: zod_1.z.object({ x: zod_1.z.number() }),
                    outputSchema: zod_1.z.any(),
                },
                invalid: {
                    name: "invalid",
                    description: "Missing schema",
                    tool: async () => null,
                },
            };
            const result = (0, registry_conversion_1.toAvailableTools)(tools);
            expect(result).toHaveLength(1);
            expect(result[0].name).toBe("valid");
            expect(warnSpy).toHaveBeenCalledWith(expect.stringContaining('Skipping tool "invalid"'));
            warnSpy.mockRestore();
        });
    });
});
//# sourceMappingURL=registry-conversion.test.js.map